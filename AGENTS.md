本项目是 OpenClaw 的外部插件，产品定位和实现边界见 `PRD.md` 与 `ARCHITECTURE.md`。

开发时建议优先阅读以下文档：
- `PRD.md`: 说明插件解决的问题、V1 范围和成功标准
- `ARCHITECTURE.md`: 说明运行时状态机、Hook 映射和模块职责
- `CLAUDE.md`: 说明当前仓库的代码结构和开发约束

联调建议：
- 使用本地可复现的 OpenClaw 环境验证插件行为
- 通过配置或环境变量注入凭据，不要把密钥、隧道命令或服务器信息写入仓库
- 在提交前确认 README、测试和 CI 与当前代码实现保持一致
